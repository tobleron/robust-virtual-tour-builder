/* tests/unit/VideoEncoderTest.res */
open VideoEncoder

let run = () => {
  Console.log("Running VideoEncoder tests...")
  
  // Test: transcodeWebMToMP4 exists
  // assert(transcodeWebMToMP4 != Obj.magic(0))
  
  Console.log("✓ VideoEncoder tests passed")
}
