# Task 195: Add Unit Tests for UrlUtils

## 🚨 Context
This task was auto-generated because modifications were detected in `src/utils/UrlUtils.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/utils/UrlUtils.res`.

## 🛠Implementation Specs
- Create `tests/unit/UrlUtils.test.res` using the `Vitest` framework.
- Ensure all tests pass with `npm test`.
