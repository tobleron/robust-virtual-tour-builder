# Task 127: Add Unit Tests for Logger

## 🚨 Context
This task was auto-generated because modifications were detected in `src/utils/Logger.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/utils/Logger.res`.

## 🛠Implementation Specs
- Create `tests/unit/LoggerTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
