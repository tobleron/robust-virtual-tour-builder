# Task 100: Add Unit Tests for SharedTypes

## 🚨 Context
This task was auto-generated because modifications were detected in `src/core/SharedTypes.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/core/SharedTypes.res`.

## 🛠Implementation Specs
- Create `tests/unit/SharedTypesTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
