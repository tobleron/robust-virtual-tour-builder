# Task 334: Update Unit Tests for ViewerLoader.res

## 🚨 Trigger
Implementation file `./src/components/ViewerLoader.res` is newer than its test file `tests/unit/ViewerLoaderTest.res`.

## Objective
Update `tests/unit/ViewerLoaderTest.res` to ensure it covers recent changes in `ViewerLoader.res`.

## Requirements
- Review recent changes in `./src/components/ViewerLoader.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.
