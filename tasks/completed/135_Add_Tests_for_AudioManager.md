# Task 135: Add Unit Tests for AudioManager

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/AudioManager.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/AudioManager.res`.

## 🛠Implementation Specs
- Create `tests/unit/AudioManagerTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
