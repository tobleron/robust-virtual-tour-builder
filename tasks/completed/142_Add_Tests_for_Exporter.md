# Task 142: Add Unit Tests for Exporter

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/Exporter.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/Exporter.res`.

## 🛠Implementation Specs
- Create `tests/unit/ExporterTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
