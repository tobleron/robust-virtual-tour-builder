# Task 1196: Create Tests for ViewerLogic System

## Objective
Create unit tests for `src/systems/ViewerLogic.res`.

## Context
`ViewerLogic.res` contains core logic for viewer manipulation (boost, edge power, etc.) but lacks specific unit tests.

## Requirements
- Create `tests/unit/ViewerLogic_v.test.res`.
- Test pure functions like `getBoost`, `getEdgePower`.
- Test interaction logic if possible.
