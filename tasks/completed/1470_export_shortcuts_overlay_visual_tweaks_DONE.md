# Task: 1470 - Export shortcuts overlay visual tweaks

## Objective
Polish exported-tour tag shortcuts and looking-mode hint UI for readability and alignment.

## Requirements
- Increase top-right white shortcut text size with responsive sizing by viewport state.
- Ensure left-side shortcut numbers align vertically with a cleaner structured layout.
- Match right-side padding of shortcut panel to export watermark/logo spacing.
- Update looking mode hint line to show `L to toggle` below title, with `L` bold and slightly larger.

## Verification
- `npm run build` succeeds.
- Exported UI reflects requested spacing, typography, and alignment updates.
