# Task 103: Add Unit Tests for Resizer

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/Resizer.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/Resizer.res`.

## 🛠Implementation Specs
- Create `tests/unit/ResizerTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
