# Task 630: Update Unit Tests for SnapshotOverlay.res

## 🚨 Trigger
Implementation file `src/components/SnapshotOverlay.res` is newer than its test file `tests/unit/SnapshotOverlay_v.test.res`.

## Objective
Update `tests/unit/SnapshotOverlay_v.test.res` to ensure it covers recent changes in `SnapshotOverlay.res`.

## Requirements
- Review recent changes in `src/components/SnapshotOverlay.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

