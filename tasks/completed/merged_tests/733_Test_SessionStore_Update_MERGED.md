# Task 733: Update Unit Tests for SessionStore.res

## 🚨 Trigger
Implementation file `src/utils/SessionStore.res` is newer than its test file `tests/unit/SessionStore_v.test.res`.

## Objective
Update `tests/unit/SessionStore_v.test.res` to ensure it covers recent changes in `SessionStore.res`.

## Requirements
- Review recent changes in `src/utils/SessionStore.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

