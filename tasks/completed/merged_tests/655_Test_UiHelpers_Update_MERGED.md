# Task 655: Update Unit Tests for UiHelpers.res

## 🚨 Trigger
Implementation file `src/core/UiHelpers.res` is newer than its test file `tests/unit/UiHelpers_v.test.res`.

## Objective
Update `tests/unit/UiHelpers_v.test.res` to ensure it covers recent changes in `UiHelpers.res`.

## Requirements
- Review recent changes in `src/core/UiHelpers.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

