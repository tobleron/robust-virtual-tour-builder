# Task 681: Update Unit Tests for ImageValidator.res

## 🚨 Trigger
Implementation file `src/systems/ImageValidator.res` is newer than its test file `tests/unit/ImageValidator_v.test.res`.

## Objective
Update `tests/unit/ImageValidator_v.test.res` to ensure it covers recent changes in `ImageValidator.res`.

## Requirements
- Review recent changes in `src/systems/ImageValidator.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

