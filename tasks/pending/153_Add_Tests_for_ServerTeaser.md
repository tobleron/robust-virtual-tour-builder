# Task 153: Add Unit Tests for ServerTeaser

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/ServerTeaser.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/ServerTeaser.res`.

## 🛠Implementation Specs
- Create `tests/unit/ServerTeaserTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
