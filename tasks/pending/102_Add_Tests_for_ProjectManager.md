# Task 102: Add Unit Tests for ProjectManager

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/ProjectManager.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/ProjectManager.res`.

## 🛠Implementation Specs
- Create `tests/unit/ProjectManagerTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
