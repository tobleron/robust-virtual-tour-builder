# Task 147: Add Unit Tests for NavigationReducer

## 🚨 Context
This task was auto-generated because modifications were detected in `src/core/reducers/NavigationReducer.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/core/reducers/NavigationReducer.res`.

## 🛠Implementation Specs
- Create `tests/unit/NavigationReducerTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
