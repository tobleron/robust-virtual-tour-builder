# Task 269: Add Unit Tests for RequestQueue

## 🚨 Context
This task was auto-generated because modifications were detected in `src/utils/RequestQueue.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/utils/RequestQueue.res`.

## 🛠Implementation Specs
- Create `tests/unit/RequestQueue.test.res` using the `Vitest` framework.
- Ensure all tests pass with `npm test`.
