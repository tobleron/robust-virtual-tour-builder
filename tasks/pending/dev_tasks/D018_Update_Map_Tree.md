# Task D018: Update MAP Directory Tree

## 🚨 Trigger
The tree block at the top of `MAP.md` no longer matches the actual repository layout.

## Objective
Bring the first code block in `MAP.md` into sync with the tracked root directories and files.

## Differences
- [ ] Remove or rename `backend/src` in the tree block (directory missing on disk).
- [ ] Remove or rename `src/bindings` in the tree block (directory missing on disk).
- [ ] Remove or rename `src/components` in the tree block (directory missing on disk).
- [ ] Remove or rename `src/core` in the tree block (directory missing on disk).
- [ ] Remove or rename `src/hooks` in the tree block (directory missing on disk).
- [ ] Remove or rename `src/i18n` in the tree block (directory missing on disk).
- [ ] Remove or rename `src/systems` in the tree block (directory missing on disk).
- [ ] Remove or rename `src/utils` in the tree block (directory missing on disk).

## Notes
- Update the tree block so this task disappears the next time `_dev-system` runs.
