# Task 146: Add Unit Tests for GlobalStateBridge

## 🚨 Context
This task was auto-generated because modifications were detected in `src/core/GlobalStateBridge.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/core/GlobalStateBridge.res`.

## 🛠Implementation Specs
- Create `tests/unit/GlobalStateBridgeTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
