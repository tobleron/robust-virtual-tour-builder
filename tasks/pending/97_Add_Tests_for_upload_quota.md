# Task 97: Add Unit Tests for upload_quota

## 🚨 Context
This task was auto-generated because modifications were detected in `backend/src/services/upload_quota.rs` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `backend/src/services/upload_quota.rs`.

## 🛠Implementation Specs
- Add `#[cfg(test)]` module to the bottom of `backend/src/services/upload_quota.rs`
- Ensure all tests pass with `npm test`.
