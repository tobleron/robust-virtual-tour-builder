# Task 138: Add Unit Tests for ProjectData

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/ProjectData.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/ProjectData.res`.

## 🛠Implementation Specs
- Create `tests/unit/ProjectDataTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
