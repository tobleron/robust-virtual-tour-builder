# Task 982: Update Unit Tests for SceneCache.res

## 🚨 Trigger
Implementation file `src/core/SceneCache.res` is newer than its test file `tests/unit/SceneCache_v.test.res`.

## Objective
Update `tests/unit/SceneCache_v.test.res` to ensure it covers recent changes in `SceneCache.res`.

## Requirements
- Review recent changes in `src/core/SceneCache.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

