# Task 644: Update Unit Tests for Actions.res

## 🚨 Trigger
Implementation file `src/core/Actions.res` is newer than its test file `tests/unit/Actions_v.test.res`.

## Objective
Update `tests/unit/Actions_v.test.res` to ensure it covers recent changes in `Actions.res`.

## Requirements
- Review recent changes in `src/core/Actions.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

