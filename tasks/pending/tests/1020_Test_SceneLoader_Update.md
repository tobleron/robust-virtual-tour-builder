# Task 1020: Update Unit Tests for SceneLoader.res

## 🚨 Trigger
Implementation file `src/systems/SceneLoader.res` is newer than its test file `tests/unit/SceneLoader_v.test.res`.

## Objective
Update `tests/unit/SceneLoader_v.test.res` to ensure it covers recent changes in `SceneLoader.res`.

## Requirements
- Review recent changes in `src/systems/SceneLoader.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

