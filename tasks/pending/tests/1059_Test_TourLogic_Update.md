# Task 1059: Update Unit Tests for TourLogic.res

## 🚨 Trigger
Implementation file `src/utils/TourLogic.res` is newer than its test file `tests/unit/TourLogic_v.test.res`.

## Objective
Update `tests/unit/TourLogic_v.test.res` to ensure it covers recent changes in `TourLogic.res`.

## Requirements
- Review recent changes in `src/utils/TourLogic.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

