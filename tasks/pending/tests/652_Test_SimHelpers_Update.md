# Task 652: Update Unit Tests for SimHelpers.res

## 🚨 Trigger
Implementation file `src/core/SimHelpers.res` is newer than its test file `tests/unit/SimHelpers_v.test.res`.

## Objective
Update `tests/unit/SimHelpers_v.test.res` to ensure it covers recent changes in `SimHelpers.res`.

## Requirements
- Review recent changes in `src/core/SimHelpers.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

