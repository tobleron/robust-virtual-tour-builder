# Task 737: Update Unit Tests for VersionData.res

## 🚨 Trigger
Implementation file `src/utils/VersionData.res` is newer than its test file `tests/unit/VersionData_v.test.res`.

## Objective
Update `tests/unit/VersionData_v.test.res` to ensure it covers recent changes in `VersionData.res`.

## Requirements
- Review recent changes in `src/utils/VersionData.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

