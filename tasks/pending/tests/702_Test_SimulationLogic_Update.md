# Task 702: Update Unit Tests for SimulationLogic.res

## 🚨 Trigger
Implementation file `src/systems/SimulationLogic.res` is newer than its test file `tests/unit/SimulationLogic_v.test.res`.

## Objective
Update `tests/unit/SimulationLogic_v.test.res` to ensure it covers recent changes in `SimulationLogic.res`.

## Requirements
- Review recent changes in `src/systems/SimulationLogic.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

