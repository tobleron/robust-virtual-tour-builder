# Task 986: Update Unit Tests for ViewerTypes.res

## 🚨 Trigger
Implementation file `src/core/ViewerTypes.res` is newer than its test file `tests/unit/ViewerTypes_v.test.res`.

## Objective
Update `tests/unit/ViewerTypes_v.test.res` to ensure it covers recent changes in `ViewerTypes.res`.

## Requirements
- Review recent changes in `src/core/ViewerTypes.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

