# Task 990: Update Unit Tests for mod.res

## 🚨 Trigger
Implementation file `src/core/reducers/mod.res` is newer than its test file `tests/unit/mod_v.test.res`.

## Objective
Update `tests/unit/mod_v.test.res` to ensure it covers recent changes in `mod.res`.

## Requirements
- Review recent changes in `src/core/reducers/mod.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

