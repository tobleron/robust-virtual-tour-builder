# Task 981: Update Unit Tests for JsonTypes.res

## 🚨 Trigger
Implementation file `src/core/JsonTypes.res` is newer than its test file `tests/unit/JsonTypes_v.test.res`.

## Objective
Update `tests/unit/JsonTypes_v.test.res` to ensure it covers recent changes in `JsonTypes.res`.

## Requirements
- Review recent changes in `src/core/JsonTypes.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

