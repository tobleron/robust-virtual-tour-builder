# Task 664: Update Unit Tests for SimulationReducer.res

## 🚨 Trigger
Implementation file `src/core/reducers/SimulationReducer.res` is newer than its test file `tests/unit/SimulationReducer_v.test.res`.

## Objective
Update `tests/unit/SimulationReducer_v.test.res` to ensure it covers recent changes in `SimulationReducer.res`.

## Requirements
- Review recent changes in `src/core/reducers/SimulationReducer.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.

