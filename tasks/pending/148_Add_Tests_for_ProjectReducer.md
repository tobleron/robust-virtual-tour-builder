# Task 148: Add Unit Tests for ProjectReducer

## 🚨 Context
This task was auto-generated because modifications were detected in `src/core/reducers/ProjectReducer.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/core/reducers/ProjectReducer.res`.

## 🛠Implementation Specs
- Create `tests/unit/ProjectReducerTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
