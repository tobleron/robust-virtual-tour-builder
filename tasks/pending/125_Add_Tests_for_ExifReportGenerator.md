# Task 125: Add Unit Tests for ExifReportGenerator

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/ExifReportGenerator.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/ExifReportGenerator.res`.

## 🛠Implementation Specs
- Create `tests/unit/ExifReportGeneratorTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
