# Task 145: Add Unit Tests for Actions

## 🚨 Context
This task was auto-generated because modifications were detected in `src/core/Actions.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/core/Actions.res`.

## 🛠Implementation Specs
- Create `tests/unit/ActionsTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
