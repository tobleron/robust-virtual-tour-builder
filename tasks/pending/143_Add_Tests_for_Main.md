# Task 143: Add Unit Tests for Main

## 🚨 Context
This task was auto-generated because modifications were detected in `src/Main.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/Main.res`.

## 🛠Implementation Specs
- Create `tests/unit/MainTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
