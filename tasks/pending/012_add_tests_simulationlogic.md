# Task 214: Add Unit Tests for SimulationLogic

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/SimulationLogic.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/SimulationLogic.res`.

## 🛠Implementation Specs
- Create `tests/unit/SimulationLogic.test.res` using the `Vitest` framework.
- Ensure all tests pass with `npm test`.
