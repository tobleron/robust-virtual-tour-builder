# Task 151: Add Unit Tests for EventBus

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/EventBus.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/EventBus.res`.

## 🛠Implementation Specs
- Create `tests/unit/EventBusTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
