# Task 94: Add Unit Tests for utils

## 🚨 Context
This task was auto-generated because modifications were detected in `backend/src/api/utils.rs` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `backend/src/api/utils.rs`.

## 🛠Implementation Specs
- Add `#[cfg(test)]` module to the bottom of `backend/src/api/utils.rs`
- Ensure all tests pass with `npm test`.
