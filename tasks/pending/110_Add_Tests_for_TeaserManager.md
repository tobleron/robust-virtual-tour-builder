# Task 110: Add Unit Tests for TeaserManager

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/TeaserManager.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/TeaserManager.res`.

## 🛠Implementation Specs
- Create `tests/unit/TeaserManagerTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
