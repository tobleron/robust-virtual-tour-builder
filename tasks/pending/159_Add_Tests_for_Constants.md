# Task 159: Add Unit Tests for Constants

## 🚨 Context
This task was auto-generated because modifications were detected in `src/utils/Constants.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/utils/Constants.res`.

## 🛠Implementation Specs
- Create `tests/unit/ConstantsTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
