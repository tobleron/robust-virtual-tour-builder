# Task 101: Add Unit Tests for BackendApi

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/BackendApi.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/BackendApi.res`.

## 🛠Implementation Specs
- Create `tests/unit/BackendApiTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
