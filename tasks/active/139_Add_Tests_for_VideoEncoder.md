# Task 139: Add Unit Tests for VideoEncoder

## 🚨 Context
This task was auto-generated because modifications were detected in `src/systems/VideoEncoder.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/systems/VideoEncoder.res`.

## 🛠Implementation Specs
- Create `tests/unit/VideoEncoderTest.res`
- Register it in `tests/TestRunner.res`
- Ensure all tests pass with `npm test`.
