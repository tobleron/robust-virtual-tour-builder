# Task: Refactor styles.css

Refactor the huge `styles.css` file into a modular structure to improve maintainability.

## Requirements
- Split `css/style.css` into logical modules.
- Use a central entry point for imports.
- Ensure no visual regressions.
- Follow functional standards (modular, predictable).

## Success Criteria
- [ ] `css/style.css` is significantly smaller and primarily contains imports.
- [ ] Modular CSS files are created in `css/` or a sub-folder.
- [ ] App still looks and functions exactly the same.
