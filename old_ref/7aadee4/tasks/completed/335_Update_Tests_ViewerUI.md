# Task 335: Update Unit Tests for ViewerUI.res

## 🚨 Trigger
Implementation file `./src/components/ViewerUI.res` is newer than its test file `tests/unit/ViewerUI_v.test.res`.

## Objective
Update `tests/unit/ViewerUI_v.test.res` to ensure it covers recent changes in `ViewerUI.res`.

## Requirements
- Review recent changes in `./src/components/ViewerUI.res`.
- Update tests to maintain 100% coverage of new logic.
- Follow /testing-standards.md.
