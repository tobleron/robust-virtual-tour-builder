# Task 215: Add Unit Tests for SessionStore

## 🚨 Context
This task was auto-generated because modifications were detected in `src/utils/SessionStore.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/utils/SessionStore.res`.

## 🛠Implementation Specs
- Create `tests/unit/SessionStore.test.res` using the `Vitest` framework.
- Ensure all tests pass with `npm test`.
