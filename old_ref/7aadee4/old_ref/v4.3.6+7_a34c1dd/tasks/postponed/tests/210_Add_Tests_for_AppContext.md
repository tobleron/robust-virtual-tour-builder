# Task 210: Add Unit Tests for AppContext

## 🚨 Context
This task was auto-generated because modifications were detected in `src/core/AppContext.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/core/AppContext.res`.

## 🛠Implementation Specs
- Create `tests/unit/AppContext.test.res` using the `Vitest` framework.
- Ensure all tests pass with `npm test`.
