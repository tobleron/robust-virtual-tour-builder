# Task 211: Add Unit Tests for UiReducer

## 🚨 Context
This task was auto-generated because modifications were detected in `src/core/reducers/UiReducer.res` without corresponding unit tests.

## 🎯 Objective
Create a unit test file to verify the logic in `src/core/reducers/UiReducer.res`.

## 🛠Implementation Specs
- Create `tests/unit/UiReducer.test.res` using the `Vitest` framework.
- Ensure all tests pass with `npm test`.
