/* src/components/ui/LucideIcons.res - Consolidated Lucide icon components */

module Play = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Play"
}

module Square = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Square"
}

module Home = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Home"
}

module Sun = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Sun"
}

module Trees = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Trees"
}

module Sprout = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Sprout"
}

module Navigation = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Navigation"
}

module FastForward = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "FastForward"
}

module ChevronRight = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "ChevronRight"
}

module ChevronUp = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "ChevronUp"
}

module ChevronsUp = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "ChevronsUp"
}

module Plus = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Plus"
}

module X = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "X"
}

module Hash = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Hash"
}

module Link = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Link"
}

module MoreVertical = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "MoreVertical"
}

module GripVertical = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "GripVertical"
}

module FilePlus = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "FilePlus"
}

module Save = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Save"
}

module FolderOpen = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "FolderOpen"
}

module Info = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Info"
}

module Share2 = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Share2"
}

module Trash2 = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Trash2"
}

module Unlink = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Unlink"
}

module ImageIcon = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
    ~stroke: string=?,
  ) => React.element = "Image"
}

module FileImage = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
    ~stroke: string=?,
  ) => React.element = "FileImage"
}

module Images = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
    ~stroke: string=?,
  ) => React.element = "Images"
}

module Film = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Film"
}

module Camera = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Camera"
}

module Sparkles = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Sparkles"
}

module BarChart3 = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "BarChart3"
}

module Download = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Download"
}

module Copy = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Copy"
}

module CircleAlert = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
    ~stroke: string=?,
  ) => React.element = "CircleAlert"
}

module CircleCheck = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
    ~stroke: string=?,
  ) => React.element = "CircleCheck"
}

module TriangleAlert = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
    ~stroke: string=?,
  ) => React.element = "TriangleAlert"
}

module Flag = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Flag"
}

module Upload = {
  @module("lucide-react") @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~fill: string=?,
  ) => React.element = "Upload"
}
